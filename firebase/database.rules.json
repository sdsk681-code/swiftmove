{
  "rules": {
    ".read": false,
    ".write": false,
    "presence": {
      ".read": "auth != null && auth.provider !== 'anonymous'",
      "$docId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    }
  }
}
